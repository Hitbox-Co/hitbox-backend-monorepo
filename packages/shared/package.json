{
  "name": "@hitbox/shared",
  "version": "0.1.0",
  "private": true,
  "description": "HitBox shared infrastructure — database, config, logger, errors, events. NO business logic.",
  "main": "./index.ts",
  "types": "./index.ts",
  "exports": {
    ".": "./index.ts"
  },
  "scripts": {
    "lint": "eslint . --ext .ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "pino": "^9.5.0",
    "zod": "^3.24.1"
  },
  "peerDependencies": {
    "express": "^4.19.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.25",
    "@types/node": "^26.1.1",
    "pino-pretty": "^11.3.0",
    "prisma": "^5.22.0",
    "typescript": "^5.6.3"
  }
}